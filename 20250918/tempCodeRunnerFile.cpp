cin >> a;
            // a--;

            // vector<ll> new_follower;
            // rep(i, N)
            // {
            //     if (i != a && (find(g.at(i).begin(), g.at(i).end(), a) != g.at(i).end()))
            //     {
            //         for (auto e : g.at(i))
            //         {
            //             if (e != a)
            //             {
            //                 new_follower.emplace_back(e);
            //             }
            //         }
            //     }
            // }

            // g.at(a).insert(g.at(a).begin(), new_follower.begin(), new_follower.end());
            // sort(g.at(a).begin(), g.at(a).end());
            // g.at(a).erase